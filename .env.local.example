# Dev Supabase — copy to .env.local and fill in from your DEV project.
# When .env.local exists, the server loads it after .env so these override production values.
# Do not commit .env.local (it is gitignored).
#
# From Settings → API: SUPABASE_URL, SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY
# From Settings → Database: SUPABASE_DB_PASSWORD (needed only for npm run dev:run-schema)

SUPABASE_URL=https://YOUR_DEV_PROJECT_REF.supabase.co
SUPABASE_ANON_KEY=your_dev_anon_key_here
SUPABASE_SERVICE_ROLE_KEY=your_dev_service_role_key_here
# SUPABASE_DB_PASSWORD=your_database_password_here

# SendGrid (for branch DB: forgot-password, admin invite emails). See SENDGRID_SETUP_WHAT_YOU_NEED.md
# Get API key: SendGrid Dashboard → Settings → API Keys. Verify sender: Settings → Sender Authentication
SENDGRID_API_KEY=your_sendgrid_api_key_here
SENDGRID_FROM_EMAIL=your_verified_sender@example.com
# SENDGRID_FROM_NAME=CrestField
# SENDGRID_REPLY_TO=support@example.com
