{
  "buildCommand": "pwd && ls -la && if [ -d client ]; then cd client && npm install && npm run build; else echo 'ERROR: client directory not found!' && ls -la && exit 1; fi",
  "outputDirectory": "client/build",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "headers": [
    {
      "source": "/assets/(.*)",
      "headers": [{ "key": "Cache-Control", "value": "public, max-age=31536000, immutable" }]
    }
  ]
}
